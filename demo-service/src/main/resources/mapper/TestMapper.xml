<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.goldleaf.dao.TestDao">
	<select id = "getById" parameterType="int" resultType="org.goldleaf.entity.TestEntity">
		SELECT name, age FROM test_user WHERE id = #{userId}
	</select>
</mapper>